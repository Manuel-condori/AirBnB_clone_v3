tags:
  - Status
summary: Returns a status message
description: Returns the status message "OK"
consumes:
  - application/json
produces:
  - application/json
responses:
  200:
    description: Success response
    schema:
      type: object
      properties:
        status:
          type: string
          description: status message
          default: OK
